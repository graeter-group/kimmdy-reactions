{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "$id": "https://raw.githubusercontent.com/hits-mbm-dev/kimmdy-reactions/main/src/homolysis/kimmdy-yaml-schema.json",
  "description": "Settings for homolysis reactions",
  "additionalProperties": false,
  "properties": {
    "edis": {
      "title": "edis",
      "deprecated": true,
      "description": "Dissociation energies data file now lives in the kimmdy root config as `config.edissoc`"
    },
    "itp": {
      "title": "itp",
      "deprecated": true,
      "description": "Additional itp file with bonded parameters is no longer required and directly read from the forcefield."
    },
    },
    "kmc": {
      "description": "KMC algorithm for this reaction.",
      "type": "string",
      "pytype": "str",
      "enum": ["rfkmc", "frm", "extrande", "extrande_mod", "multi_rfkmc"],
      "default": "rfkmc"
    },
    "check_bound": {
      "type": "boolean",
      "pytype": "bool",
      "description": "Check the topology for bonds between plumed atom pairs and only attempt to break them if they are actually bonded.",
      "default": true
    },
    },
    "arrhenius_equation": {
      "title": "arrhenius_equation",
      "type": "object",
      "description": "For setting values in the arrhenius equation",
      "properties": {
        "frequency_factor": {
          "type": "number",
          "description": "Frequency factor",
          "pytype": "float",
          "minimum": 0,
          "default": 0.288
        },
        "temperature": {
          "type": "number",
          "description": "Temperature",
          "pytype": "float",
          "minimum": 0,
          "default": 300
        }
      },
      "required": [ "frequency_factor", "temperature" ],
      "additionalProperties": false
    }
  },
  "required": [ "edis", "itp" ]
}

